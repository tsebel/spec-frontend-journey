# spec-frontend-journey

My first Vue app for the SPEC frontend challenge.

## What it does
- Displays "Hello SPEC Developers" in the browser using Vue 3

## How to run
1. Clone the repo
2. Run `npm install` to install dependencies
3. Run `npm run dev` to start the app
4. Open `http://localhost:5173/` in your browser
